import { defineStore } from "pinia";

/**
 *
 * Let's define products store.
 *
 */
export const useProductsStore = defineStore({
	id: "products",

	state: () => ({}),

	getters: {},

	actions: {},
});
