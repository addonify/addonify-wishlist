/**
 *
 * Define plugin text domain which can be used globally for translation.
 * You need to import {__} = wp.i18n to use this.
 * Usage: __( 'Text to translate', textdomain )
 *
 * @since 2.0.0
 */

export const textdomain: string = "addonify-wishlist";

/**
 *
 * JSON file name while importing and exporting.
 * This is used in import and export components.
 *
 * @since 2.0.0
 */

export const jsonFileName: string = "addonify_wishlist";

/**
 *
 * Get the current version of the plugin.
 * We are receiving the value of locolized variable.
 *
 * @since 2.0.0
 */

export const currentVersion = "2.0.0";
