/**
 *
 * Define plugin text domain which can be used globally for translation.
 * You need to import {__} = wp.i18n to use this.
 * Usage: __( 'Text to translate', textdomain )
 *
 * @since 2.0.0
 */

export const textdomain: string = "addonify-wishlist";
